提问prompt1
```
现在有一个存量vue项目在做代码规约整改。提供了2个规约集的excel，比如必须双引号，逗号结尾，2个空格缩进，不允许有console，重复率不能超过6%，函数入参不能超过5个，带默认参数必须放在最后等等，150多条。  
现在根据这些规则已经构建了prettier.config.js，eslint.config.js但还有大量问题需要人为处理，想让ai帮忙解决，搭建一套可复用的ai项目，并能自动扫描问题再解决问题再扫描，直到问题全部解决完。  
已经根据规则集制作了一个扫描的skill，里面有一个eslint项目，通过指定代码目录扫描问题，并通过python生成问题报告的excel，包括规约类型，涉及代码文件路径，行号等信息。能正常使用。  
现在的想法是让ai通过这个skill扫描的村里代码，根据生成的excel自动修改并更新状态。改完后再扫描再解决，直到所有问题清零。  
但是之前发现如果excel的问题非常多，ai改了几个就说改完了，改每个问题都非常慢。  
正对这个任务要怎么通过ai高效完成，之前制作的检查代码问题的skill有什么优化点，是否思路正确。测试怎么做，需要引入subagents/hooks这些吗，superpowers，oh-my-agent或者openspec是否有帮助
```

提问prompt2
```
1. 优化skill  
skills/code_style_scanning  
想做成通用的。  
之前结构：  
-src  
|---- package.json  
|----eslint.config.js  
|----scan.js  
-references  
---- javascript规则  
----- typescipt规则  
------ 其他。。。  
-srcipts  
---- xxx.py  
----- excel.py  
-skill.md

  

改造为最终还要生成json格式的报告，方便给ai读取。

  

2. 给项目引入prettier，eslint帮忙解决大量问题。  
3. 参数顺序，超大函数，逻辑嵌套太深这个怎么解决，codemod是什么。怎么知道哪些问题给codemod解决。  
4. 让ai修复问题，比如同一个文件有多个问题，但是如果安装excel或json清单依次解决，可能上一个解决了准备解决下一个时发现行号发生变化了怎么办。  
5. hooks怎么引入，项目都已经引入了eslint和prettier。  
6. openspec怎么引入，在哪个阶段引入，superpowers呢。  
7. 怎么ai持续不间断的工作。
```

推荐给出的结构
```
skills/
└── code_remediation/
    ├── scanner/
    │   ├── eslint.config.js
    │   ├── prettier.config.js
    │   ├── scan.js
    │   └── parsers/
    │
    ├── reports/
    │   ├── generate-json.js
    │   ├── generate-excel.py
    │   └── templates/
    │
    ├── rules/
    │   ├── formatting/
    │   ├── complexity/
    │   ├── architecture/
    │   └── duplication/
    │
    ├── codemods/
    │   ├── remove-console.js
    │   ├── reorder-default-param.js
    │   └── split-large-function.js
    │
    ├── pipelines/
    │   ├── scan.ts
    │   ├── fix.ts
    │   ├── verify.ts
    │   └── rescan.ts
    │
    ├── queue/
    │   └── tasks.json
    │
    └── skill.md
```

提问prompt3
```
1. 直接给出最终的skill结构，并标注作用  
2. 直接给出最终json结构，这个json也是通过执行code_remediation/pipelines/scan.ts得到的吗。  
3. codemod工具。存量项目大部分都是vue+js，极少使用ts，当然后续肯定会大量使用ts。怎么用呢，是集成到了code_remediation里吗还是code_remediation里规定使用jscodeshift+ts-morph处理问题，临时针对不同问题写脚本处理，js写的函数方法，改了参数顺序，调用方能同步修改吗。  
4. 同一个文件多个问题修复。根据扫描的结果就是带文件和行号的，整改也是依据扫描结果来处理的，难道还要根据文件进行二次归类整改？还没没有避免行号偏移的问题。  
5. hooks的脚本是放在什么地方，比如我使用的是opencode，放在当前项目根路径.opencode/hooks/after-fix.sh,能正常执行吗  
6. # openspec的用途还是没明白，大部分工作或者全部工作包括修复code_remediation都包含了，还需要澄清需求规则生成task执行吗。
```


# **最终推荐的 skill 结构（直接可落地）**
```
skills/
└── code_remediation/
    │
    ├── skill.md
    │
    ├── scanner/
    │   ├── eslint.config.js
    │   ├── prettier.config.js
    │   ├── scan-eslint.ts
    │   ├── scan-duplication.ts
    │   ├── scan-complexity.ts
    │   └── parsers/
    │
    ├── rules/
    │   ├── no-console.yaml
    │   ├── default-param-last.yaml
    │   ├── max-function-params.yaml
    │   ├── max-depth.yaml
    │   └── duplicated-code.yaml
    │
    ├── codemods/
    │   ├── remove-console.js
    │   ├── default-param-last.js
    │   ├── var-to-let.js
    │   └── utils/
    │
    ├── ai/
    │   ├── prompts/
    │   │   ├── split-large-function.md
    │   │   ├── reduce-nesting.md
    │   │   └── deduplicate-code.md
    │   │
    │   ├── fix-file.ts
    │   ├── batch-runner.ts
    │   └── reviewers/
    │
    ├── pipelines/
    │   ├── scan.ts
    │   ├── auto-fix.ts
    │   ├── codemod-fix.ts
    │   ├── ai-fix.ts
    │   ├── verify.ts
    │   ├── rescan.ts
    │   └── full-remediation.ts
    │
    ├── queue/
    │   ├── latest.json
    │   ├── pending.json
    │   ├── processing.json
    │   ├── fixed.json
    │   └── failed.json
    │
    ├── reports/
    │   ├── latest.xlsx
    │   ├── latest.json
    │   ├── summary.json
    │   └── history/
    │
    └── scripts/
        ├── generate-excel.py
        ├── merge-results.ts
        └── group-by-file.ts
```

```
可以直接给 Codex 下面这个 prompt：

请基于当前项目已有的 `AGENTS.md` 和 `docs/styleguide/` 下的 HTML、Vue、JavaScript、TypeScript 编码规约文档，创建一个通用代码规约整改 Skill：`skills/code_remediation/`。
## 目标
构建一个可复用的代码规约扫描与整改引擎，用于存量 Vue + JavaScript 项目的规约整改，后续也要兼容 TypeScript。
该 skill 需要支持：
1. 读取 `docs/styleguide/` 下的规约文档
2. 建立规则元数据
3. 扫描指定代码目录
4. 输出 JSON 报告，供 AI / pipeline 读取
5. 输出 Excel 明细报告，供人工查看
6. 区分不同修复策略：
   - prettier/eslint
   - codemod
   - ai
   - manual
7. 支持后续按文件聚合问题，避免行号漂移
8. 为后续自动整改闭环预留 pipeline
## 请创建以下目录结构
```txt
skills/
└── code_remediation/
    ├── skill.md
    │
    ├── scanner/
    │   ├── eslint.config.js
    │   ├── prettier.config.js
    │   ├── scan-eslint.js
    │   ├── scan-styleguide.js
    │   └── parsers/
    │
    ├── rules/
    │   ├── html.yaml
    │   ├── vue.yaml
    │   ├── javascript.yaml
    │   └── typescript.yaml
    │
    ├── reports/
    │   ├── generate-json.js
    │   ├── generate-excel.py
    │   ├── latest.json
    │   ├── latest.xlsx
    │   └── history/
    │
    ├── queue/
    │   ├── latest.json
    │   ├── pending.json
    │   ├── fixed.json
    │   └── failed.json
    │
    ├── codemods/
    │   ├── README.md
    │   ├── remove-console.js
    │   ├── default-param-last.js
    │   └── utils/
    │
    ├── ai/
    │   ├── prompts/
    │   │   ├── fix-file.md
    │   │   ├── reduce-nesting.md
    │   │   ├── split-large-function.md
    │   │   └── deduplicate-code.md
    │   └── README.md
    │
    ├── pipelines/
    │   ├── scan.js
    │   ├── auto-fix.js
    │   ├── codemod-fix.js
    │   ├── ai-fix.js
    │   ├── verify.js
    │   ├── rescan.js
    │   └── full-remediation.js
    │
    └── scripts/
        ├── group-by-file.js
        ├── merge-results.js
        └── validate-report.js

规则处理要求

请读取并分析：

docs/styleguide/

下的所有 Markdown 文件。

将规则归类为：

formatting
eslint
codemod
ai
manual

判断原则：

* 能被 prettier 处理的，标记为 formatting
* 能被 eslint –fix 处理的，标记为 eslint
* 能通过 AST 机械转换处理的，标记为 codemod
* 需要理解业务语义、函数拆分、重复逻辑抽取、嵌套逻辑重构的，标记为 ai
* 无法安全自动修复的，标记为 manual

JSON 报告格式

pipelines/scan.js 最终需要生成：

{
  "scan_id": "scan_YYYYMMDD_HHmmss",
  "project": "project-name",
  "created_at": "ISO_TIME",
  "target": "src",
  "summary": {
    "total": 0,
    "formatting": 0,
    "eslint": 0,
    "codemod": 0,
    "ai": 0,
    "manual": 0,
    "fixed": 0,
    "pending": 0,
    "failed": 0
  },
  "tasks": [
    {
      "id": "task-000001",
      "rule": "no-console",
      "category": "eslint",
      "severity": "error",
      "file": "src/views/home.vue",
      "line": 42,
      "column": 8,
      "message": "Unexpected console statement",
      "source": "eslint",
      "fix_strategy": "eslint",
      "fixable": true,
      "status": "pending",
      "batch_id": null,
      "retry_count": 0,
      "created_at": "ISO_TIME"
    }
  ],
  "files": [
    {
      "file": "src/views/home.vue",
      "total": 10,
      "tasks": ["task-000001"]
    }
  ]
}

Excel 报告要求

同时生成 Excel 明细报告：

skills/code_remediation/reports/latest.xlsx

Excel 至少包含以下列：

* task_id
* rule
* category
* severity
* file
* line
* column
* message
* source
* fix_strategy
* fixable
* status
* batch_id
* retry_count

扫描命令要求

请在 skills/code_remediation/pipelines/scan.js 中实现命令行参数：

node skills/code_remediation/pipelines/scan.js --target src

输出：

skills/code_remediation/reports/latest.json
skills/code_remediation/reports/latest.xlsx
skills/code_remediation/queue/latest.json
skills/code_remediation/queue/pending.json

不要做的事

不要直接大规模修改业务代码。

这次只创建 skill 框架、扫描管线、报告格式、规则元数据和示例 codemod。

不要把所有整改逻辑都交给 AI。

不要依赖旧行号长期追踪问题。

整改时后续应按文件聚合问题，再整体修复该文件，然后重新扫描。

验收标准

完成后请确保：

1. skills/code_remediation/skill.md 清楚说明使用方式
2. pipelines/scan.js --target src 可以执行
3. 能生成 JSON 报告
4. 能生成 Excel 报告
5. 能按文件生成 files 聚合信息
6. 规则 YAML 中能体现来自 docs/styleguide/ 的规则
7. 不修改业务代码
8. 如果缺少依赖，请更新 package.json 或给出明确安装命令


请先实现最小可运行版本，不要一次性实现完整自动修复闭环。第一阶段只要求 scan + json report + excel report + rule metadata。
```


```
请使用 `skills/code_remediation` 对当前存量 Vue + JavaScript 项目执行规约整改。

目标是尽可能自动完成整改，但必须以安全、可验证、可回滚为优先。

运行要求：

1. 所有运行产物输出到 `.code-remediation/`
2. 不要把项目扫描结果写入 `skills/code_remediation/`
3. 先执行全量扫描
4. 先处理 prettier/eslint 可自动修复问题
5. 再处理 codemod 可修复问题
6. 最后处理 AI 语义重构问题
7. AI 修复必须按文件分组，不允许按旧行号逐条修改
8. 每批最多处理 3 个文件
9. 每批完成后必须执行 verify 和 rescan
10. 如果 verify 失败，回滚本批修改并写入 failed.json
11. 如果 rescan 后问题没有减少，停止当前批次并记录原因
12. 不要声称全部完成，除非 pending.json 为空或只剩 manual
13. 超大函数、深层嵌套、重复代码、参数过多属于 ai_fix，必须小批量处理
14. 不要进行全项目大重构
15. 每轮输出当前统计：total、pending、fixed、failed、manual

请先从初始化 `.code-remediation/`、执行 scan、生成 latest.json/latest.xlsx 开始，然后进入自动整改循环。
```


## 新功能
```
能否给这个skill再内置几种整改场景的工作流。类似如下  
- 基础：使用skill内置的工具比如eslint等无风险的整改。输出剩余问题及建议  
- 上强度：在“基础”的基础上使用codemod等工具再处理一批低风险的问题。输出剩余问题及建议。  
- 极致：高度自动化，按照指定流程完成问题整改，全程不用人工参与。
```


## 新功能
```
如果整改的问题很多，会分批处理，是否可以已经多个子agent帮忙处理，不然在同一个会话下会导致上下文太长。

请为 `skills/code_remediation` 增加多 worker 批处理能力。

要求：

1. 将 pending tasks 按文件分组生成 batches
2. 每个 batch 包含 1~3 个文件
3. 支持多个 worker 并发处理 batch
4. 不允许两个 worker 同时处理同一个文件
5. 增加 `.code-remediation/locks/` 文件锁机制
6. 每个 batch 处理完成后必须执行 verify
7. verify 失败必须回滚该 batch 并写入 failed.json
8. verify 成功后更新 batch 状态为 verified
9. 一轮 batch 全部完成后执行 rescan
10. 不要让单个 worker 读取全部 pending.json
11. worker 只能读取自己 batch 对应的文件和任务
12. 所有运行产物写入 `.code-remediation/`
```